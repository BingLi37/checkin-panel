[pytest]
# Only the panel's own suite is in this repository; the desktop shell's tests are kept with
# the development tree. `asyncio_mode = auto`, so an async test needs no decorator.
testpaths = panel/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
